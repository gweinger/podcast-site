name = "gweinger-site"
compatibility_date = "2024-01-01"

# Note: the contact form's email send lives in a standalone Worker
# (workers/contact/) routed at gweinger.com/api/contact — Pages Functions
# cannot use the send_email binding.

[env.production]
